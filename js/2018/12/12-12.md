```javascript
function cameLizer (str) {
    return str
                .split('-')
                .map((v, i) => i === 0 ? v : v[0].toUpperCase() +  v.slice(1))
                .join('')
}
console.log(cameLizer('my-short-thing'))
```
